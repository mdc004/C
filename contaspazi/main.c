#include <stdio.h>
#include <stdlib.h>

int confronta(char prima[50], char seconda[50]);
int main()
{
    printf("\nInserire la prima stringa: ");
    gets(uno);
    printf("\nInserire la seconda stringa: ");
    gets(due);
    ver=confronta(prima,seconda);
    if(ver==1)printf("\nLe stringhe sono uguali\n");
    else printf("\nLe stringhe non sono uguali\n");
    return 0;
}

int confronta(char prima[50], char seconda[50]){
    int i, x;
    char prima[50], seconda[50];


}
